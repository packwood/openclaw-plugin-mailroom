{
  "id": "mailroom",
  "name": "Mailroom",
  "description": "Build evolving Agent Responsibility Profiles, route Outlook email to the best OpenClaw agent, draft with authorized context, and manage human approval in Telegram.",
  "activation": {"onStartup": true},
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dbPath": {
        "type": "string",
        "description": "SQLite ledger path. Defaults to ~/.openclaw/mailroom/mailroom.db."
      },
      "pythonPath": {
        "type": "string",
        "description": "Optional Python module root. Defaults to the package's bundled python directory."
      },
      "pythonExecutable": {
        "type": "string",
        "default": "python3"
      },
      "telegramChatId": {
        "type": "string",
        "minLength": 1,
        "description": "Telegram chat authorized to approve Mailroom actions."
      },
      "telegramThreadId": {
        "type": "string",
        "minLength": 1,
        "description": "Optional Telegram forum-topic thread ID for Mailroom cards."
      },
      "telegramDestinations": {
        "type": "object",
        "additionalProperties": {
          "type": "object",
          "additionalProperties": false,
          "required": ["chatId"],
          "properties": {
            "chatId": {
              "type": "string",
              "minLength": 1,
              "description": "Telegram chat ID for this agent's Mailroom cards."
            },
            "threadId": {
              "type": "string",
              "minLength": 1,
              "description": "Optional Telegram forum-topic thread ID for this agent's Mailroom cards."
            }
          }
        },
        "description": "Per-owner Telegram destinations for Mailroom cards. Keys are OpenClaw agent IDs. Owners without an entry use telegramChatId/telegramThreadId."
      },
      "routingReviewTelegramAccountId": {
        "type": "string",
        "minLength": 1,
        "default": "default",
        "description": "Telegram account used only for ambiguous routing-review cards. Set to Orchestrator's Telegram account; defaults to default."
      },
      "routingReviewAgentId": {
        "type": "string",
        "minLength": 1,
        "default": "main",
        "description": "OpenClaw agent whose owner cards use routingReviewTelegramAccountId. Its Telegram account binding must route that account to the same agent."
      },
      "reviewOwners": {
        "type": "array",
        "items": {"type": "string"},
        "default": [],
        "description": "OpenClaw agent IDs used when routingOwnerMode is selected. Also retained as an upgrade fallback."
      },
      "routingOwnerMode": {
        "type": "string",
        "enum": ["all", "selected"],
        "default": "all",
        "description": "Make every profiled/discovered OpenClaw agent routable, or only reviewOwners."
      },
      "profilesPath": {
        "type": "string",
        "pattern": "(^|[/\\\\])current\\.json$",
        "description": "Effective responsibility-profile JSON. Must end in current.json; defaults to ~/.openclaw/mailroom/responsibility-profiles/current.json."
      },
      "accounts": {
        "type": "object",
        "additionalProperties": {"type": "string"},
        "default": {},
        "description": "Optional map of friendly account IDs to mailbox email addresses."
      },
      "connectionsPath": {
        "type": "string",
        "description": "Path to OpenClaw's Outlook connection registry."
      },
      "signaturesPath": {
        "type": "string",
        "description": "Optional directory containing <account>.html signatures."
      }
    }
  }
}
